# Prometheus — мониторинг (стек)
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['127.0.0.1:9090']

  # Добавьте сюда endpoint приложения при экспорте метрик:
  # - job_name: 'api'
  #   static_configs:
  #     - targets: ['host.docker.internal:3001']
